name: stop_the_bleeding
description: "Stop the Bleeding!"
publish_to: "none"
version: 0.1.0+1

environment:
   sdk: ^3.11.0

dependencies:
   flutter:
      sdk: flutter
   sqflite: ^2.3.3
   path: ^1.9.0

dev_dependencies:
   flutter_test:
      sdk: flutter
   flutter_lints: ^6.0.0
   flutter_launcher_icons: ^0.14.4

flutter:
   uses-material-design: true

flutter_launcher_icons:
   android: true
   ios: true
   image_path: "burning-cash.png"
